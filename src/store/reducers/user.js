/*
  根据prevState和action来生成newState
*/
const initUser = {};

export default function user(prevState = initUser, action) {
  switch (action.type) {
    default:
      return prevState;
  }
}
